<Window x:Class="PL.Engineer.EngineerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Engineer"
        mc:Ignorable="d"
        Title="EngineerWindow" Height="450" Width="800"
        DataContext="{Binding add_updateEngineer , RelativeSource={RelativeSource Self}, Mode=TwoWay}">
    
   

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="19" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <Label Content="ID :" Margin="46,25,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Label Content="Name :" Margin="46,102,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.04,-0.385" HorizontalAlignment="Left"/>
        <Label Content="Email :" Margin="46,174,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Label Content="Level :" Margin="46,251,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Label Content="Cost :" Margin="46,317,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Label Content="Task-ID work on :" Margin="46,383,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Visibility="{Binding ElementName=Button,Path=content,Converter={StaticResource ConvertTextToVisibility}}"/>
        <TextBox HorizontalAlignment="Left" Margin="221,33,0,0" TextWrapping="Wrap" Width="120" VerticalAlignment="Top"
                 Text="{Binding  add_updateEngineer.Id, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}"/>
        <TextBox Margin="221,114,0,0" TextWrapping="Wrap" Width="120" HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding  add_updateEngineer.Name, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}"/>
        <TextBox Margin="221,186,0,0" TextWrapping="Wrap" Width="120" HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding  add_updateEngineer.Email, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}"/>
        <ComboBox HorizontalAlignment="Left" Margin="221,257,0,0" Width="120" VerticalAlignment="Top"
                  ItemsSource="{Binding Source={StaticResource LevelCollectionKey}}" SelectedValue="{Binding level, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>
        <TextBox HorizontalAlignment="Left" Margin="221,325,0,0" TextWrapping="Wrap"  Width="120" VerticalAlignment="Top"
                 Text="{Binding  add_updateEngineer.Cost, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}"/>
        <TextBox HorizontalAlignment="Left" Margin="221,391,0,0" TextWrapping="Wrap" Width="120" VerticalAlignment="Top"
                 Text="{Binding  add_updateEngineer.Task.Id, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" Visibility="{Binding ElementName=Button,Path=content,Converter={StaticResource ConvertTextToVisibility}}/>
        <Button Content="{Binding Id, Mode=OneTime, Converter={StaticResource ConverIdToContent}}" HorizontalAlignment="Left" Margin="697,383,0,0" VerticalAlignment="Top" Height="38" Width="93" Click="Button_Click" />
   </Grid>

</Window>
